/* Chestnut Adventure 2.5D — supporting cast. The models live in models.js (one design language);
   this file only keeps the old names the scenes call. */
(function () {
  'use strict';
  const CH = window.CH;
  const M = CH.models;
  CH.actors = { ted: M.ted, vera: M.vera, cat: M.cat, dusty: M.dusty, magpie: M.magpie };
})();
